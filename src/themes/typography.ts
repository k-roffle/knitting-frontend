import { createMuiTheme } from '@material-ui/core';

export const { typography } = createMuiTheme({
  typography: {
    h2: {
      fontSize: 18,
      fontWeight: 500,
    },
    h6: {
      fontSize: 15,
      fontWeight: 500,
    },
  },
});
